# ══ PERSONAL PROJECTS — never push ══
src/backend/
src/frontend/
src/mobile/
src/db/
src/integrations/
src/shared/

# ══ CONFIDENTIAL CLIENT WORK — never push ══

# ══ PERSONAL DOCS — project-specific ══
docs/

# ══ PERSONAL KNOWLEDGE BASE ══
knowledge/
# ...except the protocol + lessons files the framework needs to run on a fresh
# clone. Everything else under knowledge/ (courses, notes, plans) stays local.
!knowledge/protocols/
!knowledge/protocols/*.md
!knowledge/protocols/*.py
!knowledge/lessons-learned/
!knowledge/lessons-learned/PLAYBOOK.md

# ══ SECRETS ══
.env
*.env
secrets/
*.key
*.pem
*.p12
*.pfx
id_rsa
id_ed25519
*.secret

# ══ LOCAL CLAUDE MEMORY ══
.claude/
agents/**/.claude/

# ══ TEMP / SCRATCH ══
org/test_write.tmp
org/FILE_ACTIVITY.md
*.tmp

# ══ macOS ══
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# ══ Node / Python artefacts ══
node_modules/
__pycache__/
*.pyc
dist/
build/
.next/
.nuxt/
.output/

# ══ Editor ══
.vscode/settings.json
.idea/
*.swp
*.swo

# ══ Logs ══
npm-debug.log*
.DS_Store
**/.DS_Store

# Local-only backups of site snapshots — never commit, never publish

# Local-only work tool — contains employer client names and production DB queries. NEVER commit.

# Browser test artifacts (session scratch, not source)
.playwright-mcp/
**/.playwright-mcp/
badges-after-resize.png

# ── Operational logs: machine-local, NEVER committed ─────────────────
# These accumulate real client names, project detail and internal
# decisions. This repo is PUBLIC. Templates live in org/templates/;
# bin/bootstrap-org copies them into place on a fresh clone.
org/ACTIVITY.md
org/ACTIVITY_ADDENDUM.md
org/DECISIONS.md
org/COMPANY_LOG.md
org/STATUS.md
org/BLOCKERS.md
org/LIVE.md
org/RISKS.md
org/HANDOFF.md
org/FILE_ACTIVITY.md
org/AGENT_PERFORMANCE.md
org/AGENT_CHANGELOG.md
org/bugs/
review/SIGN_OFFS.md

# Browser/test scratch
**/.playwright-mcp/
**/*-screenshot.png
agents/*/recipe-chain.png
agents/*/badges-after-resize.png

# Rescued working state from deleted clones — local only, never published

# ── local/ — everything machine-only lives here and is never pushed ──────
# working clones, client work, local tools, backups, rescued state, and the
# offline practice exam. See local/README.md.
local/
